x = [1.0, 2.2, 3.0];
yvals = [3.0,6.0,4.0];
xi = 1.1;
lagrange_quadratic(x, yvals,xi);

xi = 1.75;
lagrange_quadratic(x, yvals,xi);

xi = 2.0;
lagrange_quadratic(x, yvals,xi);

xi = 2.2;
lagrange_quadratic(x, yvals,xi);

xi = 2.5;
lagrange_quadratic(x, yvals,xi);